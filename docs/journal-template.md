# Journal entry template

Copy this file. Every heading below is required, in this order.

**The claim.** One paragraph with the numbers inline. A reader who stops here still learns something
true.

## What we tried

Including the attempts that failed and what was abandoned, because those are the most useful part for
a reader.

## What we changed

## Evidence

Units on every quantity. Sources named. Any number measured in a different session or on different
hardware is labelled as such.

Every figure is committed under `docs/figures/<entry-slug>/` in this repository (its convention is
`docs/figures/README.md`) and referenced here by absolute URL, not as a relative path. Each entry
ships with at least one chart and at least one diagram or render, each captioned with what it encodes.

## What this does not establish

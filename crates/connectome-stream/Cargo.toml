[package]
name = "qualia-connectome-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Binary framing for the Male CNS positions artifact and the per-tick spike stream"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

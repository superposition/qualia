[package]
name = "qualia-sync-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Typed sync.v1 envelopes, CRDT merge rules, and authority policies for Qualia"

[dependencies]
qualia-types = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# ===========================================
# QUALIA — Environment Configuration
# ===========================================

# Robot connection (for monitoring/SSH)
ROBOT_HOST=192.168.1.100
ROBOT_USER=ubuntu

# ROS Bridge WebSocket URL
ROS_BRIDGE_URL=ws://localhost:9090

# IPFS (optional — for passport storage)
IPFS_API_URL=http://localhost:5001

# Agent registry (optional — for on-chain registration)
# Supports any EVM chain
RPC_URL=http://localhost:8545
CHAIN_ID=31337
REGISTRY_ADDRESS=
